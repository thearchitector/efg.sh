services:
  app:
    container_name: efg
    build: .
    ports:
      - 8000:8000
    volumes:
      - .:/app
      - /.venv
      - /.pdm-python
    depends_on:
      - db

  db:
    image: postgres:alpine
    environment:
      POSTGRES_PASSWORD: password
